// V4 voice intake reuses v3's voice surface (shares the gemini-live token route).
// For Hybrid+path, the voice prompt uses the generic hybrid system prompt.
export { VoiceIntake, type VoiceIntakeHandle } from "@/components/v3/voice-intake";
